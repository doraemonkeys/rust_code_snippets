[package]
name = "rust-to-c"
version = "0.1.0"
edition = "2021"
#  Cargo 会先编译和执行该构建脚本，然后再去构建整个项目。
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
